[spice "shorthand"]
	can = ca --no-edit
	la = ls -a
	lla = ll -a

[spice "branchCheckout"]
	showUntracked = true

[spice "branchPrompt"]
	sort = committerdate

[spice "log"]
	pushStatusFormat = aheadBehind

# vim:set ft=gitconfig:
