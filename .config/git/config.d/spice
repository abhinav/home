[spice "shorthand"]
	can = ca --no-edit
	la = ls -a
	lla = ll -a

	# Open the diff for the given branch in revdiff.
	review = !$HOME/tools/git-spice-revdiff "$@"

[spice "submit"]
	navigationCommentSync = downstack

[spice "branchCreate"]
	generatedBranchNameLimit = 64

[spice "branchCheckout"]
	showUntracked = true

[spice "branchPrompt"]
	sort = committerdate

[spice "log"]
	pushStatusFormat = aheadBehind
[spice "experiment"]
	commitFixup = true
	commitPick = true
	merge = true

# vim:set ft=gitconfig:
