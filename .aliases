#!/bin/bash

if command -v exa &>/dev/null; then
	alias ls=exa
fi

alias l="ls"
alias ll="ls -l"
alias la="ls -a"
alias lla="ls -la"

if [ "$EDITOR" = "nvim" ]; then
	alias vi=nvim
	alias vim=nvim
	alias vimdiff='nvim -d'
fi

alias g=git
