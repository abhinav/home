[package]
name = "yabaictl"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.71"
clap = { version = "4.3.11", default-features = false, features = ["std", "help", "usage", "error-context", "derive"] }
serde = { version = "1.0.171", features = ["derive"] }
serde_json = "1.0.100"
